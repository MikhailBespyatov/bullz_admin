import { TailLayoutType } from 'components/common/buttons/Button/types';

export const tailLayout: TailLayoutType = {
    wrapperCol: { offset: 8, span: 16 }
};

export const bottomPadding = '5px';
