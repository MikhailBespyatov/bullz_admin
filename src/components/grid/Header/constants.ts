import { padding } from 'constants/styles/sizes';

export const logoHeight = '30px';
export const logoWidth = `calc(2 * ${logoHeight})`;
export const avatarDiameter = logoHeight;

export const headerMarginRight = `calc(2 * ${padding})`;

export const copyIconDiameter = '16px';
export const popupWrapperPaddingLeft = padding;
export const popupWrapperWidth = `calc(${copyIconDiameter} + ${popupWrapperPaddingLeft})`;
